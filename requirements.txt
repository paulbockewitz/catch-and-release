# Core utilities used by most tools
python-dotenv>=1.0.0       # Load .env into os.environ automatically

# Google APIs (enable as needed)
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.0.0

# Data handling
pandas>=2.0.0
openpyxl>=3.1.0            # Excel read/write support for pandas

# HTTP / scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# CLI helpers
argparse                   # stdlib, listed here as a reminder — no pip install needed
